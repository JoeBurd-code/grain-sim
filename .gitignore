# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Playwright MCP artefacts (screenshots + page snapshots land in the repo root)
.playwright-mcp/
*-screenshot.png
page-*.png
page-*.yml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.env*

# Source PDFs dropped in docs/ for reference during analysis — the durable
# record is the .md writeup made from them, not the PDF itself.
docs/*.pdf

# Fillable follow-up worksheet for the engineer — a working doc, not a
# durable record; answers get merged into OPEN_QUESTIONS.md / lineData.js.
docs/TREATER_LINE2_OPEN_QUESTIONS.html
